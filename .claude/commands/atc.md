Read `skills-for-code/atc-code.md` from the repo root and execute the ATC protocol defined there.

**Triggers:** `run ATC`, `atc`, `ops check`, `shift review`, `quality check`, `run air traffic control`, `check on the workers`

ATC is the shift supervisor — never executes work tasks. Monitors Wingman sessions, audits file integrity, manages the ClickUp queue, surfaces anomalies as ClickUp tasks.

$ARGUMENTS
